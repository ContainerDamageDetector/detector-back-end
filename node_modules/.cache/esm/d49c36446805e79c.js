let express,validateToken,models;_680‍.x([["default",()=>_680‍.o]]);_680‍.w("express",[["default",["express"],function(v){express=v}]]);_680‍.w("../middlewares/auth",[["validateToken",["validateToken"],function(v){validateToken=v}]]);_680‍.w("../models",[["default",["models"],function(v){models=v}]]);


const { customAlphabet } = require("nanoid");
const sgMail = require("@sendgrid/mail");
const { SENDGRID_API_KEY } = require("../config");

const router = express.Router();

router.get("/all", async (req, res) => {
  try {
    const users = await models.User.findAll({ include: { all: true } });
    res.status(200).json(users.map((user) => user.toUserJson()));
  } catch (error) {
    res.status(400).send(error.message);
  }
});

router.post("/create", async (req, res) => {
  try {
    const { username, fullName, email, password, roles } = req.body;

    const isValid = models.User.validateUserData({
      username,
      fullName,
      email,
      password,
      roles,
    });
    if (!isValid) throw new Error("Invalid Data");
    const user = await models.User.create({
      username,
      fullName,
      email,
      roles,
      passwordHash: await models.User.hashPassword(password),
    });
    res.status(200).json(user.toUserJson());
  } catch (error) {
    res.status(400).send(error.message);
  }
});

router.get("/:id", validateToken, async (req, res) => {
  try {
    const user = await models.User.findByPk(req.params.id);
    res.status(200).json({ data: user.toUserJson() });
  } catch {
    res.status(400).send(error.message);
  }
});

router.put("/updateUser", validateToken, async (req, res) => {
  try {
    const { id, username, fullName, email } = req.body;
    const user = await models.User.findOne({
      where: { id },
    });
    if (!user) throw new Error("Invalid User");
    const isValid = models.User.validateUserData({
      username,
      fullName,
      email,
    });
    if (!isValid) throw new Error("Invalid Data");
    user.set({
      username,
      fullName,
      email,
    });
    await user.save();
    res.status(200).json(user.toUserJson());

    await user.save();
    res.status(200).json(user.toUserJson());
  } catch {
    res.status(400).send(error.message);
  }
});

router.post("/changePassword", async (req, res) => {
  try {
    const { id, password } = req.body;
    const user = await models.User.findOne({
      where: { id },
    });
    if (!user) throw new Error("Invalid User");
    const isValid = models.User.validatePassword(password);
    if (!isValid) throw new Error("Invalid Data");
    user.set({
      passwordHash: await models.User.hashPassword(password),
    });
    await user.save();
    res.status(200).json(user.toUserJson());
  } catch (error) {
    res.status(400).send(error.message);
  }
});





_680‍.d(router);
